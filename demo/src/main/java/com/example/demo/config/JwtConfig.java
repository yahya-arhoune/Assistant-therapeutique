package com.example.demo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JwtConfig {

    // This class is optional but useful for clarity and future extensions
    // JWT values are usually stored in application.properties
}
